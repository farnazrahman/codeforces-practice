#include<stdio.h>
int main(){
   int n,t;
   scanf("%d %d",&n,&t);

   char arr[n+1];
   scanf("%s",arr);
while(t--){
for(int i=0;i<n-1;i++){
    if(arr[i] == 'B' && arr[i+1] ==  'G'){
        arr[i] = 'G';
        arr[i+1] = 'B';
        i++;
    }
}
}printf("%s",arr);
    return 0;
}